pragma solidity ^0.8.20;
contract FreeVote {
    mapping(uint256 => uint256) public votes;
    event Voted(address voter, uint256 id);

    function vote(uint256 id) external {
        votes[id]++; 
        emit Voted(msg.sender, id);
    }
}
