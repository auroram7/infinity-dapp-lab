// SPDX-License-Identifier: MIT
pragma solidity ^0.8.20;

contract PingPong {
    string public last;
    function ping() external { last = "ping"; }
    function pong() external { last = "pong"; }
}
