// SPDX-License-Identifier: MIT
pragma solidity ^0.8.20;

contract Reaction {
    mapping(uint256 => uint256) public likes;
    event Reacted(address user, uint256 id);

    function react(uint256 id) external {
        likes[id] += 1;
        emit Reacted(msg.sender, id);
    }
}
