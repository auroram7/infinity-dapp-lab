// SPDX-License-Identifier: MIT
pragma solidity ^0.8.20;

contract AlwaysDonate {
    event Received(address from, uint256 value);
    receive() external payable { emit Received(msg.sender, msg.value); }
}
